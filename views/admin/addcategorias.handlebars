{{#each erros}}
<div class="alert alert-danger">{{texto}}<a href=""><span><i class="far fa-times-circle"></i></span></a></div>
{{else}}
{{/each}}

<header>
    <header class="masthead" style="background: linear-gradient(to right, #0062E6, #33AEFF); height:350px;">
        <div class="overlay"></div>
        <div class="container">
            <div class="row">
                <div class="col-lg-8 col-md-10 mx-auto">
                    <div class="page-heading">
                        <h2>Nova categoria</h2>
                    </div>
                </div>
            </div>
        </div>
    </header>
</header>


{{>_arrow-left}}
<div class="container mt-2">
    <div class="card">
        <div class="card-body">
            <form action="/admin/categorias/nova" method="POST">
                <label for="nome">Nome:</label>
                <input type="text" id="nome" name="nome" placeholder="Nome da categora" class="form-control" required>

                <label for="slug">Slug:</label>
                <input type="text" id="slug" name="slug" placeholder="Slug da categoria" class="form-control " required>

                <button type="submit" class="btn btn-success mt-4">Criar categoria</button>
            </form>
        </div>
    </div>

</div>